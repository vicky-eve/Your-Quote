import { QuoteVotesDirective } from './quote-votes.directive';

describe('QuoteVotesDirective', () => {
  it('should create an instance', () => {
    const directive = new QuoteVotesDirective();
    expect(directive).toBeTruthy();
  });
});
